1.The current method of saving the classier does block the tornado IOLoop. The model.save function is not wrapped in any async IOLoop function to run on a different thread. 

2. No because model.save saves the models on the local machine but it's not saved as a document in the MongoDB server. In order to access the models in a migrated MongoDB, we would have to save the models as a document specifically.  